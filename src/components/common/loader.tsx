const Loader = () => {
  return (
    <div className="w-full h-full fixed insets-0 flex items-center justify-center bg-primary text-white">
      <span className="text-[64px] md:text-[100px]">Clot</span>
    </div>
  );
};

export default Loader;
