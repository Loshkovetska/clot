const Loader = () => {
  return (
    <div className="w-full h-full fixed left-0 right-0 top-0 flex items-center justify-center bg-primary text-white">
      <span className="text-[64px] md:text-[100px]">Clot</span>
    </div>
  );
};

export default Loader;
